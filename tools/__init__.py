# tools/ holds every deterministic tool from architecture.md - no LLM calls in
# this package, just plain code (API calls, math, file parsing). Agent 1 and
# the hallucination check are the only two places in the app that call an LLM.
